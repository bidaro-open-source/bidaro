export const roles = {
  USER: 'user',
  SUPERUSER: 'superuser',
} as const

export const permissions = {
  VIEW_OWN_SESSIONS: 'view_own_sessions',
  DELETE_OWN_SESSIONS: 'delete_own_sessions',
} as const
